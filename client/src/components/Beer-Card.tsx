import React from 'react';

interface Props {
  name: string;
  price: number;
  description: string;
  active: boolean;
}

const BeerCard: React.FC<Props> = () => {
  return <></>;
};

export default BeerCard;
